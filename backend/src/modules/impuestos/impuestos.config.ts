/**
 * Tasas de impuesto por tipo de ingreso.
 *
 * IMPORTANTE: estos valores son un PLACEHOLDER, no una tasa fiscal real.
 * Ajusta estos números según la regla que corresponda a tu caso.
 * Se expresan como decimal: 0.10 = 10%.
 *
 * Este archivo es el ÚNICO lugar donde debes cambiar la tasa de impuesto.
 * No mezcles este valor dentro del componente de Angular ni del controller.
 */

export const TASA_IMPUESTO_EXTRA = 0.10;    // 10% sobre SUELDO_EXTRA — AJUSTA ESTE VALOR
export const TASA_IMPUESTO_VARIADO = 0.10;  // 10% sobre SUELDO_VARIADO — AJUSTA ESTE VALOR